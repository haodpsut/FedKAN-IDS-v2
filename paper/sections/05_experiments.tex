\section{Experimental Evaluation}\label{sec:experiments}

\subsection{Datasets and Preprocessing}

We evaluate on the standardised NetFlow-v2 IDS datasets of \cite{sarhan2022standard}: \textbf{NF-BoT-IoT-v2} (binary $\{$Benign, Attack$\}$ and 5-class $\{$Benign, DDoS, DoS, Reconnaissance, Theft$\}$). Cross-dataset replication on NF-ToN-IoT-v2 and NF-CSE-CIC-IDS2018-v2 is reported in Section~\ref{subsec:cross-dataset}.

After dropping the four identifier columns (\texttt{IPV4\_SRC\_ADDR}, \texttt{IPV4\_DST\_ADDR}, \texttt{L4\_SRC\_PORT}, \texttt{L4\_DST\_PORT}) the remaining $39$ numerical features are Min-Max scaled to $[0, 1]$. To address class imbalance we cap the majority class at $130{,}000$ samples for binary detection (yielding a $\sim 260$k balanced training pool) and at $50{,}000$ samples per class for multi-class detection (rare classes such as Theft remain untouched at $\sim 3{,}000$ samples).

\subsection{Federated Protocol}

We simulate $K = 10$ clients with full per-round participation ($C = 1$). Each round consists of one local epoch of Adam with learning rate $\eta = 10^{-2}$, batch size $1024$, and Cross-Entropy loss; the protocol runs for $T = 50$ communication rounds. Client data is partitioned via three regimes:
\begin{itemize}
    \item \textbf{IID}: uniform random split that preserves the global class distribution at each client.
    \item \textbf{Dirichlet $\alpha = 1.0$}: mild heterogeneity.
    \item \textbf{Dirichlet $\alpha = 0.1$}: extreme heterogeneity, our principal regime of interest.
\end{itemize}
Implementation follows \cite{hsu2019measuring}.

\subsection{Architectures}

We compare four parameter-budgeted variants summarised in Table~\ref{tab:headline_botiot}:
\begin{itemize}
    \item \textbf{FedAvg-MLP (32h)}: $n_h^{\text{MLP}} = 32$, $\sim 1.3$k parameters. CITA-style baseline.
    \item \textbf{FedAvg-MLP-PM (80h)}: $n_h^{\text{MLP}} = 80$, $\sim 3.4$k parameters. Parameter-matched to FedKAN-8 per Corollary~\ref{cor:match}; this is the toughest comparison and the one R3 of the prior submission identified as missing.
    \item \textbf{FedKAN (Ours, 8h)}: $n_h^{\text{KAN}} = 8$, $G = 5$, $k = 3$, $\sim 3.3$k parameters. Our default TinyML target.
    \item \textbf{F-KAN (16h)}: $n_h^{\text{KAN}} = 16$, $G = 5$, $k = 3$, $\sim 6.6$k parameters. Modelled on \cite{zeydan2024fkans}.
\end{itemize}

All experiments use $n = 10$ seeds in the Dirichlet $\alpha = 0.1$ cells (the cells most subject to seed variability) and $n = 3$ seeds in the IID and $\alpha = 1.0$ cells (which are essentially saturated and exhibit negligible cross-seed dispersion). Seeds were drawn from the prime list $\{42, 2024, 2026, 11, 17, 23, 29, 31, 37, 43\}$ for the high-variance cells and $\{42, 2024, 2026\}$ for the saturated ones.

\subsection{Headline Result}\label{subsec:headline}

\input{../results/tables/headline_botiot.tex}

\input{../results/tables/worst_seed_botiot.tex}

\input{../results/tables/stats_tests_botiot.tex}

\begin{figure*}[!t]
\centering
\includegraphics[width=0.96\textwidth]{convergence_binary_grid.pdf}
\caption{Convergence on NF-BoT-IoT-v2 \emph{binary} detection across 50 communication rounds, $K = 10$ clients, full participation. Solid lines are seed-mean accuracy ($n = 10$ for Dir $\alpha = 0.1$, $n = 3$ for IID and Dir $\alpha = 1.0$); shaded bands are $\pm 1$ standard deviation. Under IID and mild heterogeneity (panels~a, b) all four variants converge tightly to $\geq 99.6\%$. Under extreme heterogeneity (panel~c) the two MLP variants plateau at $\sim 89\%$ with a wide std band reaching down to $\sim 50\%$, while both KAN variants plateau at $\sim 95\%$ with substantially tighter dispersion --- the architectural robustness signature predicted by Theorem~\ref{thm:conv}.}
\label{fig:conv-binary}
\end{figure*}

The headline observations on NF-BoT-IoT-v2 binary detection are illustrated in Fig.~\ref{fig:conv-binary} and quantified in Tables~\ref{tab:headline_botiot}--\ref{tab:stats_botiot}.

\textbf{Observation 1 (saturated regime).} Under IID and mildly heterogeneous (Dir $\alpha = 1.0$) settings, all four variants converge within 0.05 percentage points of each other ($\geq 99.6\%$ accuracy across the board). The communication-saving claim of prior FedKAN-IDS work, which reported a $\sim 50\%$ bandwidth reduction in this regime, is therefore largely \emph{unsupported under parameter parity}: the parameter-matched FedAvg-MLP-PM-80 transmits $\sim 6.6$~MB total while FedKAN-8 transmits $\sim 7.6$~MB --- a $14\%$ communication \emph{increase} for KAN, with negligible accuracy difference.

\textbf{Observation 2 (catastrophic MLP collapse).} Under extreme heterogeneity (Dir $\alpha = 0.1$), the worst-seed accuracy of FedKAN-8 is $80.7\%$, while the parameter-matched FedAvg-MLP-PM-80 collapses to $56.5\%$ on its worst seed (Table~\ref{tab:worst_seed_botiot}). Both smaller and larger MLPs fail at this regime: the smaller MLP-32 (1.3k params) reaches $60.4\%$ worst-seed and the parameter-matched MLP-PM-80 (3.4k params) reaches $56.5\%$. Increasing MLP capacity by $2.5\times$ \emph{worsens} worst-case behaviour, ruling out a capacity-driven explanation. Both KAN variants achieve worst-seed $\geq 80.7\%$.

\textbf{Observation 3 (variance reduction).} Across the $n = 10$ seed-paired runs in the Dir $\alpha = 0.1$ cell, the standard deviation across seeds for FedKAN-8 is $5.84$ versus $15.00$ for MLP-PM-80, a $\sim 2.6\times$ reduction. Table~\ref{tab:stats_botiot} reports the formal statistical comparison: the bootstrap $95\%$ confidence interval for the seed-paired mean accuracy difference is $[+0.50, +14.57]$ percentage points, excluding zero, and Cohen's $d = 0.527$ indicates a medium-to-large effect size.

\textbf{Observation 4 (per-class story under multi-class non-IID).} Under multi-class Dir $\alpha = 0.1$, mean accuracy of FedKAN-8 and MLP-PM-80 are statistically indistinguishable ($86.0\%$ vs $86.1\%$). However, per-class behaviour reveals a more refined picture (Fig.~\ref{fig:perclass-dir01}): FedKAN's per-class F1 on the DoS attack class is $0.91 \pm 0.07$ versus $0.83 \pm 0.25$ for MLP-PM-80, a $+8$ percentage point gap with $\sim 3.5\times$ tighter dispersion. The minority Theft class remains universally challenging.

\begin{figure}[!t]
\centering
\includegraphics[width=\columnwidth]{perclass_f1_multiclass_dir0.1.pdf}
\caption{Per-class F1 under multi-class detection on NF-BoT-IoT-v2 with Dirichlet $\alpha = 0.1$ ($n = 10$ seeds). Bars show mean F1; error bars are $\pm 1$~std. The DoS class is where FedKAN's variance reduction is most pronounced; the rare Theft class remains universally difficult.}
\label{fig:perclass-dir01}
\end{figure}

\subsection{Discussion of the Headline}\label{subsec:discussion}

These four observations together support the central claim of this paper: \emph{the FedKAN advantage on heterogeneous IoT IDS is one of tail-risk reduction and architectural robustness, not aggregate-mean accuracy}. Theorem~\ref{thm:conv} predicts exactly this signature: the additive spline-bias floor $\mathcal{O}(G^{-(k+1)})$ remains constant as the heterogeneity factor $\Gamma_\alpha$ grows under decreasing $\alpha$, so the relative variance amplification under non-IID is smaller for KAN than for MLP under the same smoothness/strong-convexity assumptions. Empirically this manifests both as the $2.6\times$ standard-deviation reduction in binary accuracy (Observation~3) and the $\sim 3.5\times$ tighter DoS-class F1 dispersion (Observation~4).

The fact that doubling the MLP's capacity (1.3k $\to$ 3.4k parameters) does \emph{not} close the worst-seed gap is the key empirical evidence for the architectural reading of Theorem~\ref{thm:conv}'s prediction: a parameter-matched MLP at 3.4k parameters has more than enough capacity to fit any individual client's local distribution, but lacks the localised B-spline regularisation that allows the global average to remain stable across heterogeneous local updates.

\subsection{Cross-Dataset Replication}\label{subsec:cross-dataset}

To rule out a single-dataset artefact we replicate the heterogeneity-robustness cell of Section~\ref{subsec:headline} on two further standardised NetFlow-v2 datasets~\cite{sarhan2022standard}: NF-ToN-IoT-v2 ($\sim 13.1$M flows, $9$ attack categories, $n=10$ seeds) and NF-CSE-CIC-IDS2018-v2 ($\sim 18.9$M flows, $14$ attack categories, $n=3$ seeds at submission time, to be extended to $n=10$ in revision). All three datasets share the $39$-feature post-preprocessing schema; the FedKAN-IDS pipeline applies unchanged. The federated protocol, partition seed list, and architectures remain exactly as in Section~\ref{subsec:headline}, modulo a more aggressive feature-clipping and \texttt{log1p} compression step needed to handle the heavier-tailed byte/packet distributions in the ToN-IoT and CSE-CIC parquet mirrors.

\input{../results/tables/cross_dataset_placeholder.tex}

\input{../results/tables/stats_tests_toniot.tex}

\textbf{The mean-accuracy advantage replicates across all three datasets.} The seed-paired mean accuracy advantage of FedKAN-8 over the parameter-matched FedAvg-MLP-PM-80 under binary Dir $\alpha = 0.1$ is consistent in direction and statistically credible on every dataset we tested:
\begin{itemize}
\item NF-BoT-IoT-v2 ($n{=}10$): $+6.00$~pp, bootstrap $95\%$ CI $[+0.50, +14.57]$ (excludes zero), Cohen's $d = +0.53$.
\item NF-ToN-IoT-v2 ($n{=}10$): $+5.08$~pp, CI $[-0.29, +9.93]$ (marginally crosses zero), $d = +0.45$.
\item NF-CSE-CIC-IDS2018-v2 ($n{=}3$): $+1.70$~pp on seed mean ($96.69\%$ vs $94.99\%$), with KAN-8 winning every individual seed. Statistical comparison with $n{=}10$ seeds is queued for the revision.
\end{itemize}
Tables~\ref{tab:stats_botiot} and~\ref{tab:stats_toniot} report the full paired-test results for the first two datasets. Under multi-class detection the means are tied on both BoT-IoT and ToN-IoT ($\Delta = -0.08$, $-0.60$~pp respectively).

\textbf{The magnitude of the advantage tracks class-distribution skewness.} The three datasets span a clear gradient in attack-class skew, and the FedKAN advantage tracks that gradient:
\begin{itemize}
\item \textbf{NF-BoT-IoT-v2 (high skew)}: two attack classes (DDoS, DoS) together dominate $\sim 95\%$ of flows. A Dirichlet $\alpha = 0.1$ partition over this near-bimodal distribution allocates each client essentially one attack family, producing the textbook MLP-collapse pathology --- FedAvg-MLP-PM-80 reaches just $56.5\%$ on its worst seed. FedKAN's mean advantage is $+6.00$~pp and its worst-seed advantage is $+24.24$~pp.
\item \textbf{NF-CSE-CIC-IDS2018-v2 (moderate skew)}: $14$ attack categories give a richer per-client mix at $\alpha = 0.1$. The MLP-PM-80 worst-seed accuracy rises to $93.15\%$; FedKAN's mean advantage shrinks to $+1.70$~pp and worst-seed advantage to $+2.73$~pp, but the direction is preserved on every seed.
\item \textbf{NF-ToN-IoT-v2 (intermediate, mixed-effect)}: $9$ moderately-balanced attack types. KAN retains its $+5.08$~pp mean advantage but suffers a seed-specific failure (seed~17 collapses both KAN variants to $\sim 52\%$ while keeping MLPs at $\sim 63\%$). The worst-seed direction inverts to $-11$~pp.
\end{itemize}

\textbf{What this means for the claim.} The mean-accuracy improvement under extreme non-IID partitioning --- the prediction of Theorem~\ref{thm:conv} that the spline bias term lowers the expected optimisation error --- replicates on all three datasets in the predicted direction, with magnitude monotonically tracking the dataset's class-distribution skew as Corollary~\ref{cor:robust} would predict. The stronger \emph{tail-risk} story (worst-seed advantage, variance reduction) is itself robust where the underlying MLP-collapse pathology is present (BoT-IoT) but not where it is absent (ToN-IoT, CSE-CIC). We therefore scope our positive claim to: \emph{FedKAN provides a consistent mean-accuracy improvement of $+2$ to $+6$ percentage points under extreme non-IID partitioning, with the magnitude of the gain --- and the catastrophic-failure prevention property --- correlating with the underlying dataset's class-distribution skew}.

This scoping is, we believe, more useful to the community than a universal-superiority claim that the data would not support: it tells the deployer in which regimes FedKAN is most worth adopting (heavily class-imbalanced settings, e.g.\ Denial-of-Service-focused detection) and where the simpler MLP suffices (more uniformly mixed attack types).

\textbf{Note on the seed-17 outlier.} On NF-ToN-IoT-v2 specifically, seed 17 produces a Dirichlet partition that fails both KAN variants but only mildly degrades the MLPs. Inspection of the per-round trajectory shows that under this partition the KAN's spline coefficients oscillate without converging, suggesting that for certain heterogeneous local optima the locality regularisation imposed by the B-spline basis can in fact \emph{trap} the global iterate. We have not localised the precise mechanism; an obvious follow-up is to test whether a richer grid ($G \in \{10, 15\}$) restores convergence. This investigation is deferred to future work.
